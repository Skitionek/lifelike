apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mycelium.fullname" . }}-test-connection"
  labels:
    {{- include "mycelium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mycelium.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
